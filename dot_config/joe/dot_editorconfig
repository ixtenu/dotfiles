[*]
# JOE rc files use hard tabs to minimize differences with upstream rc files.
indent_style = tab
indent_size = 8
# The upstream rc files have trailing whitespace; preserve it in the modified
# files to minimize differences.
trim_trailing_whitespace = false
